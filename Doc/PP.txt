포고스턱을 따라하되 기존 2d 방식에서 3d 방식으로 전환.
3인칭으로.


 로직

캐릭터 클래스 또는 폰 클래스 상속.
캐릭터(상체)와 콩콩이(하체) 콜리전 생성, 
기본 위치 변경(이동) 삭제.(입력만 제거해도 무방)


 콩콩이 로직

땅에 닿으면 자동으로 점프.
단, 점프는 z값을 높히는게 아니라 요벡터 방향(캐릭터 머리방향)으로 좌표이동.(GetUpVector 함수)
땅에 닿았을때 스페이스바를 누르고 있다면 점프 안하고 대기.(hit event)
대기시간이 길수록 점프높이 증가. 대기시간은 최대 2초로

w와s로 롤값 변경(AddActorLocalRotation)
a와d로 피치값 변경(롤과 피치값이 변경되므로 요벡터 방향이 변함)
//회전축 2개 이용하면 축이 뒤틀림 해결방법 찾는중


공중에 있을시 몸 중앙기준으로 변경(피봇 포인트 중앙)
땅에 있을시 캐릭터 발바닥을 기준으로 변경(피봇 포인트 발바닥)  
 


점프했을때 주인공의 회전값(롤 and 피치 위치)과 높이값(y값) 저장.
공중에 있을때 최대 높이 저장.( == 땅에 있을때 최대높이 초기화)
다음번 땅에 닿기 전까지 롤이나 피치값이 한번 회전했다면(각각 따로)
점프높이 변화
if (공중 최대 높이<기본높이 +회전 보너스값)
   기본높이+회전 보너스값 
else 
공중 최대 높이

바닥이 경사져 있다면 
1.
경사진 방향으로 점프시 벡터 추가
벡터크기는 점프크기와 정비례하게 
(line trace 함수 활용)

2.
(90-입사각)만큼 피치 , 롤 값 변경(미끄러짐 구현)
미끄러짐은 아주 짧은시간에 속도가 빠르게 점감하게 구현
(line trace 함수 활용2)


캐릭터(상체)로직

땅에 닿았을경우 입사각 방향으로 좌표이동(튕겨나감) 
line trace

땅과 캐릭터가 충돌할경우 충돌 반대 방향으로 회전(튕겨돌아감) 
component hit 이벤트 -> break hit point -> nomal vector


점프로 넘어갈수 있는 다양한 지형 지물을 만들고
캐릭터가 골인 지점에 도착하면 승리.







여유있을때 구현할것들
1. 3단점프(슈퍼마리오)
2. 밟히면 데미지를 받는 몬스터(슈퍼마리오)
3. 아이스 지형(회전 미끄러짐은 구현 x)
4. 바람
5. 콩콩이 방향으로 총쏘기
6. 보스전



